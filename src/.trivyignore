# .trivyignore
# CVEs aceitos com justificativa documentada.
# Revise periodicamente — remova a entrada quando uma versão corrigida for publicada.

# CVE-2026-32286 / GO-2026-4518: github.com/jackc/pgproto3/v2 — Denial of Service via malicious PostgreSQL server
# Status: sem versão corrigida disponível (affected, no fix).
# Vetor: requer que o atacante controle o servidor PostgreSQL.
# Mitigação: o serviço conecta exclusivamente ao próprio banco controlado por nós,
#             não há exposição a servidores PostgreSQL externos ou não confiáveis.
CVE-2026-32286
GO-2026-4518
